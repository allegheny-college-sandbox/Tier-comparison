name: 'Tier Comparison'
description: 'Run Tier Comparison SheetShuttle plugin'
runs:
  using: "composite"
  steps:
    - uses: actions/checkout@v3
    - uses: actions/setup-python@v4
    - run: python ReadWriteGoogleSheet.py
      shell: bash
    - run: python plugins/github_issues.py
      shell: bash
